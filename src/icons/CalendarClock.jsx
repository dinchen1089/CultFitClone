import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const CalendarClockIcon = ({...props}) => {
  return <Icon name={'calendar-clock'} size={24} {...props} />;
};

export default CalendarClockIcon;
