import Icon from 'react-native-vector-icons/MaterialIcons';

const ChevronLeftIcon = ({...props}) => {
  return <Icon name={'chevron-left'} size={24} {...props} />;
};

export default ChevronLeftIcon;
