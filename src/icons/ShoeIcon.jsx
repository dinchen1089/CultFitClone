import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const ShoeIcon = ({...props}) => {
  return <Icon name={'shoe-sneaker'} size={24} {...props} />;
};

export default ShoeIcon;
