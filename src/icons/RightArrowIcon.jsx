import Icon from 'react-native-vector-icons/MaterialIcons';

const RightArrowIcon = ({...props}) => {
  return <Icon name={'arrow-forward'} size={24} {...props} />;
};

export default RightArrowIcon;
