import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const QuoteRightIcon = ({...props}) => {
  return <Icon name={'format-quote-close'} size={24} {...props} />;
};

export default QuoteRightIcon;
