import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const KettlebellIcon = ({...props}) => {
  return <Icon name={'kettlebell'} size={24} {...props} />;
};

export default KettlebellIcon;
