import Icon from 'react-native-vector-icons/MaterialIcons';

const FitnessIcon = ({...props}) => {
  return <Icon name={'directions-run'} size={24} {...props} />;
};

export default FitnessIcon;
