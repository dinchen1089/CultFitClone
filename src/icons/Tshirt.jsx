import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const TShirtIcon = ({...props}) => {
  return <Icon name={'tshirt-crew'} size={24} {...props} />;
};

export default TShirtIcon;
