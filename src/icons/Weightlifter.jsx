import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const WeightLifterIcon = ({...props}) => {
  return <Icon name={'weight-lifter'} size={24} {...props} />;
};

export default WeightLifterIcon;
