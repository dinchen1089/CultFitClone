import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const NutritionIcon = ({...props}) => {
  return <Icon name={'nutrition'} size={24} {...props} />;
};

export default NutritionIcon;
