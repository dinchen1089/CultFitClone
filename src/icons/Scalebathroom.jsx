import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const ScaleBathroomIcon = ({...props}) => {
  return <Icon name={'scale-bathroom'} size={24} {...props} />;
};

export default ScaleBathroomIcon;
