import Icon from 'react-native-vector-icons/Foundation';

const ClipBoardIcon = ({...props}) => {
  return <Icon name={'clipboard-pencil'} size={24} {...props} />;
};

export default ClipBoardIcon;
