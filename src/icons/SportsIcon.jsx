import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
const SportsIcon = ({...props}) => {
  return <Icon name={'sports-handball'} size={24} {...props} />;
};

export default SportsIcon;
