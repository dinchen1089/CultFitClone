import React from 'react';
import Icon from 'react-native-vector-icons/Feather';

const HomeIcon = ({...props}) => {
  return <Icon name={'home'} size={24} {...props} />;
};

export default HomeIcon;
