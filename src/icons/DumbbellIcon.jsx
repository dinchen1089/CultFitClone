import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const DumbbellIcon = ({...props}) => {
  return <Icon name={'dumbbell'} size={24} {...props} />;
};

export default DumbbellIcon;
